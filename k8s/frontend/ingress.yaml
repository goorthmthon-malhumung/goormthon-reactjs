apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: junhugaeapi.goorm.training
      http:
        paths:
          - backend:
              service:
                name: backend-service
                port:
                  number: 80
            path: /
            pathType: Prefix
    - host: junhugae.goorm.training
      http:
        paths:
          - backend:
              service:
                name: frontend-service
                port:
                  number: 80
            path: /
            pathType: Prefix
