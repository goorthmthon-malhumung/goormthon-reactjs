apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: goormthon-1

resources:
  - frontend.yaml
  - ingress.yaml

configMapGenerator:
  - name: frontend-config
    files:
      - .env=config/.env

generatorOptions:
  disableNameSuffixHash: true
